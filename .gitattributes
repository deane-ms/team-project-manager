# Shell scripts must stay LF even on Windows checkouts -- a CRLF line ending turns the shebang
# into "/bin/sh\r", which fails with a "bad interpreter" error and silently disables the hook.
.githooks/* text eol=lf
*.sh text eol=lf
